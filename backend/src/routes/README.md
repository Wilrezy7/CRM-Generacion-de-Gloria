# Routes

Tabla de rutas REST. `server.js` mantiene el enrutamiento actual sin dependencias externas; los nuevos endpoints de mentorias y auditoria ya siguen el contrato documentado en `docs/multiusuario-mentorias.md`.
